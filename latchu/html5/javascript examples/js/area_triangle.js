const baseValue=prompt('enter base of a triangle');
const heightValue=prompt('enter Height of a triangle');
const areaValue=(baseValue*heightValue)/2;
console.log(`area of the triangle is ${areaValue}`);